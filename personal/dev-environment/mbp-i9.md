---
context: personal
---
# Dev Environment: mbp-i9
**Last Updated:** 2026-02-19
**Summary:** MacBook Pro i9 15" (Intel). ⬜ Partially filled — verify on next active session.

---

## Machine Specs

| Item | Value |
|------|-------|
| OS | macOS (Intel) |
| Model | MacBook Pro 15" (i9) |
| Hostname | ⬜ confirm |
| Architecture | x86_64 |

---

## Environment Profile

| Variable | Value |
|----------|-------|
| Home directory | `/Users/mcglothi/` (assumed — confirm) |
| Code root | `~/code/` (assumed — confirm) |
| Package manager | `brew` (Homebrew — assumed) |
| Init system | `launchd` |
| Python command | `python3` (version ⬜) |
| Shell | `zsh` |
| Architecture | `x86_64` |

---

## Tool Versions
| Tool | Version | Notes |
|------|---------|-------|
| Python | ⬜ | |
| Git | ⬜ | |
| Homebrew | ⬜ | |

---

## Not Installed (cross-machine hazards)
Same as mbp-m1 — macOS machine, no pacman/systemd/PipeWire/OpenRGB.

---

## Instructions for AI
**Next time a session is active on this machine:**
1. Run `hostname` and update the hostname above.
2. Run `sw_vers` and `uname -m` to confirm OS and arch.
3. Run `which python3 && python3 --version`, `git --version`, `brew --version` and fill in Tool Versions.
4. Confirm home dir and code root, update Environment Profile if different from assumed values.
